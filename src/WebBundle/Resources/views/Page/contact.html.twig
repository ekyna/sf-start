{% extends "WebBundle:Page:default.html.twig" %}

{% block containers %}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            {{ cms_breadcrumb() }}
            {{ cms_title() }}
            {%- include "EkynaCoreBundle:Ui:flashes.html.twig" -%}
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            {{ cms_content() }}
        </div>
        <div class="col-md-6">
            {%- include "WebBundle:Page:_contact_form.html.twig" -%}
        </div>
    </div>
</div>
{% endblock containers %}
